
  # Redesign visual da interface

  This is a code bundle for Redesign visual da interface. The original project is available at https://www.figma.com/design/Z03MkML7dZKdLHwyLNAN6c/Redesign-visual-da-interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  